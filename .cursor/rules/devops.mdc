---
description: CI/CD pipeline patterns, deployment workflows, quality gates, and release management.
---

# DevOps Standards

## Pipeline Architecture

- **Stages:** Construct robust pipelines with standard stages: `Install` -> `Verify` (Lint/Test) -> `Preview` -> `Deploy`
- **Caching:** Aggressively cache `node_modules` based on `bun.lock` hash to speed up builds
- **Tool Chain:** Use project-local binaries (`bun x wrangler`) to ensure version consistency. Avoid global installs in CI

## Quality Gates

- **Blocker:** No code reaches usage (Preview or Prod) without passing QA protocols (Linting, Types, Unit Tests)
- **Preview:** Every Merge Request should generate a unique, ephemeral Preview URL for stakeholder verification
- **Automation:** Run `bun run lint`, `bun run typecheck`, and `bun run test:unit` in CI
- **iOS Automation:** For changes under `ios/` or mobile API/auth/billing contracts, run `bun run ios:check` on a macOS runner with full Xcode, an iOS Simulator runtime, and XcodeGen installed

## Deployment Safety

- **Migrations:** Database migrations (`bun run db:migrate:prod`) must run **successfully** before the new code is promoted
- **Secrets:** Inject sensitive config via Masked/Protected CI Variables. Never log these
- **Order:** Always run migrations before deploying new code that depends on schema changes
- **Feature flags:** Ship flag-gated code with the Flagship dashboard flag **disabled** first. Push to `main` deploys code (Workers Builds) but Flagship controls exposure. No Flagship API calls in `.gitlab-ci.yml`. Emergency kill: `FEATURE_FLAG_OVERRIDES` secret (JSON)

## Deployment Workflow

1. **Generate Migrations:** `bun run db:generate` (checks if schema needs updating)
2. **Apply Migrations:** `bun run db:migrate:prod` (applies to remote D1 database)
3. **Build:** `bun run build` (generates production build)
4. **Deploy:** `bun run deploy` (builds + deploys to Cloudflare)

## iOS Build/Test Lane

Use package scripts so local developers, Cursor agents, and CI use the same commands:

1. **Generate Xcode project:** `bun run ios:generate`
2. **Build app:** `bun run ios:build`
3. **Run XCTest:** `bun run ios:test`
4. **Full iOS gate:** `bun run ios:check`

The default destination is `generic/platform=iOS Simulator` for CI portability. Developers can target a concrete simulator, for example:

```bash
IOS_DESTINATION="platform=iOS Simulator,name=iPhone 17" bun run ios:check
```

Any Swift file addition/removal or `ios/project.yml` change requires regenerating `ios/Ration.xcodeproj`; build failures like "Cannot find X in scope" often mean Xcode is still using a stale generated project.

## CI/CD Configuration

- **Test/verify pipeline:** `.gitlab-ci.yml` runs `install` → `verify` (lint/typecheck/unit tests) stages. Its own `deploy` stage is commented out (`#- deploy`) and is **not** the deploy mechanism — do not assume this file controls production deploys.
- **Actual deploy mechanism:** Cloudflare **Workers Builds** is connected directly to the GitLab repo and watches `main`. On every push to `main`, Cloudflare triggers its own build+deploy job on a GitLab runner, entirely outside `.gitlab-ci.yml`. This means `git push` to `main` (e.g., the final step of `/quick-commit`) is itself a production deploy trigger — treat it accordingly.
- **Known reliability gap:** these Cloudflare-triggered auto-deploys sometimes fail, and Cloudflare rolls back to the previously deployed Worker version on failure — **silently, with no alerting configured**. A route or config change can therefore be merged to `main` and pass all local/CI checks while never actually reaching production, with no signal that it didn't. Treat any prod-vs-code discrepancy as a possible silent rollback before assuming a missing manual step. Adding deploy failure/rollback alerting (e.g., via Cloudflare Workers Builds notifications/webhooks) plus a post-deploy smoke test of critical routes is an open hardening item.
- **Scripts:** Use `package.json` scripts for consistency between local and CI environments
- **Versioning:** Ensure `wrangler.jsonc` compatibility_date is up-to-date

## Blue/Green Deployment

- **Pattern:** Consider Blue/Green deployment patterns for zero-downtime deployments
- **Rollback:** Have a rollback plan ready (revert migrations + code if needed)
